@page "/counter"

<h1>Count</h1>

<p>Current count: @currentCount</p>

<button @onclick="IncrementCount">Click to Increase Count</button>
<button @onclick="@(e => IncrementCount())">Click to Show Message</button>

@code {
    private int currentCount = 0;

    void IncrementCount()
    {
        currentCount++;
        var a = new Layout();
    }
}